function onEdit(e) {
  DesktopEntry.onEdit(e);
}

function refreshAll() {
  DesktopEntry.refreshAll();
}

function doGet(e) {
  return ApiEntry.doGet(e);
}

function doPost(e) {
  return ApiEntry.doPost(e);
}
